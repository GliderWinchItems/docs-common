Software TODO items--

This list is to capture items we talked about that are lower on 
the todo list and easily forgotten.

1. 01/12/26 
- CAN msgs for calibrated and uncalibrated tension
  Add CAN msg for zero'd calibrated tension
  CP F4 or drum reads POD msg; recalibrates and sends CAN msg;
    adds zero'ing offset from a cangate command,
    or, CP pushbutton sequence.

 - Belt tension in GEVCUr or MC startup that holds up
   applying torque without the belt being tensioned.

- Ztension
  Cangate command, or CP pushbutton sequence.

- Odometer
  iFGT - sheave with Drum direction

- Rope speed
  iFGT sheave with drum direction or,
  drum encoder calibrated by sheave for
  high resolution at slow speed.


2. 01/13/26

-  (Earlier) BMS modules do not run low current chargers when
   winch state is in not-SAFE.

   Add to this not to charge when there is no AC power (which
   would mean the 12v supplies are running on the HV battery).

   Tentative: Have BMS command to not charge for 'x' seconds
   when a CAN msg from the emcmmc is sent. The emcmmc determines
   the conditions when charging is off and sends these msgs
   fast enough to hold the BMS nodes from charging.

3. 01/23/26

- 004 msg from gps/logger requires changing CAN id B1C00000 in gps/logger
  as it conflicts withe BMS module on GSM's battery string.

  The gps/logger used this address for NSAT, and other items in
  the lat/long/height reporting.

  The GliderWinchCommons/embed/svn_common/trunk/db/CANID_INSERT.sql
  has the gps/logger CAN id set for B1C00004 (29b), but these
  have not been updated on the loggers.
